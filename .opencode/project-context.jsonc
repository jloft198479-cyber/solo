{
  // solo — 轻量 Markdown 编辑器
  // 项目路径: F:\fzz-Project\md-editor\md-editor
  //
  // 全面档案见同目录 PROFILE.md，新会话先读那篇
  //
  // 本文件用于多 agent 快速索引，不保留细节。
  // 所有问题、决策、影响范围统一记录在 PROFILE.md 对应章节。

  "techStack": {
    "frontend": "Vue 3 + Pinia + TypeScript",
    "editor": "TipTap v3 (ProseMirror)",
    "markdownParseSerializer": "自研 parser/serializer（非 prosemirror-markdown）",
    "desktop": "Tauri v2.11 / Rust 1.96.0",
    "build": "bun run build → bunx tauri build",
    "packageManager": "bun v1.3.14"
  },

  "profile": ".opencode/PROFILE.md",

  "completedTasks": [
    // ── 窗口/生命周期系列 (v1.2.1) ──
    "Phase A: Rust on_window_event 合并 close+focus，前端 blur→destroy/focus→lazyInit",
    "Phase B: Editor 懒初始化 — onMounted 不创建，等 focus/click",
    "Bug 1-3: set_focus / setTimeout 兜底 / @click.self→@click",
    "P0: isSaving 锁阻塞关闭时保存 — saveCurrentDocument 等待≤1s",
    "P1: LoadedWindows/FocusedWindow 不清理 — Destroyed 清理",
    "P2: 菜单退出不退出进程 — exit_app Rust 命令",
    "P4: FULLSCREEN 从 StateFlags 移除",

    // ── 粘贴/复制/导出系列 (v1.2.2) ──
    "Fix #5: 通用 Markdown 粘贴检测 — 标题/引用/围栏/列表",
    "Fix #6: Ctrl+C 带 Markdown 源码 — clipboardTextSerializer",
    "Fix #7: 确认「导出 PDF」→「打印」",

    // ── 文件关联系列 (v1.2.3) ──
    "v1.2.3: 文件关联移入 NSIS installerHooks — 安装即生效",

    // ── Markdown roundtrip 系列 (ffa2d2a) ──
    "嵌套列表序列化：子列表前空行破坏嵌套 → serializer renderContent 增加 inTightList 判断",
    "嵌套列表序列化：子列表无缩进 → renderList 增加 listDepth 和 3sp/级 缩进",
    "3 层嵌套 roundtrip 不稳定 → 同上两处修复",
    "任务列表 checked 状态丢失 → parser html_inline 提取 checked=\"\" 设到 parent taskItem",
    "任务列表 checkbox 后文本双空格 → inline handler 跳过前导空格",
    "test schema 缺 taskList/taskItem 节点 → test-utils.ts + roundtrip.spec.ts 补充",

    // ── Roundtrip Fix B — !? 过度转义 + CJK 标点加粗边界 (2026-06-29) ──
    "Fix A: serialzer escapeInline 正则移除 !? —— 非 CommonMark 特殊字符，消除 2 个 roundtrip 失败",
    "Fix B: CJK 标点后 **/* 无法关闭 — serializer renderMarks 边界检测 + ZWNJ (U+200C) 插入，覆盖 bold + italic，消除 3 个 roundtrip 失败",
    "新增 _isUnicodePunctOrSym + _delimiterBoundaryUnsafe 方法到 serializer.ts",
    "新增 ZWNJ reopen 测试用例到 roundtrip.spec.ts",

    // ── 主题系统 (ffa2d2a) ──
    "highlight mark 样式硬编码 → 提取为 theme markBg / markBorderRadius CSS 变量",

    // ── blockquote vs callout 视觉差异化 ──
    "blockquote 引用 和 callout 重点 左边框+背景色一模一样 → blockquote 保留边框底色，callout 去边框改纯背景卡片 + ::before 类型标签",

    // ── v1.2.4 ──
    "callout border-radius 0（去圆角）+ 加重底色（light 0.08→0.11, dark 0.10→0.12），7主题预设同步",
    "版本 1.2.4 发布：solo_1.2.4_x64-setup.exe",

    // ── v1.2.5: 全进程卡死根因修复 ──
    "拆除 tauri-plugin-single-instance，改为多进程独立架构",
    "main.rs: setup_webview2_data_dir() — 每进程独立 %TEMP% EBWebView 目录 + 24h 过期清理",
    "lib.rs: 删除 EARLY_OPEN_REQUEST / dispatch_or_store_open_request / store_early_open_request / take_early_open_request / single-instance 插件注册 / setup 早期请求恢复",
    "models.rs: AppOpenSource 删除 Startup / SingleInstance 变体",
    "RunEvent::Opened 改为直接 create_editor_window",
    "修复 macOS 闭包参数名不匹配的潜伏编译 bug",
    "版本 1.2.5 发布：solo_1.2.5_x64-setup.exe（编译通过）"
  ],

  "keyDecisions": {
    // ── v1.2.5: 多进程独立架构 ──
    "multiProcessInsteadOfSingleInstance": "tauri-plugin-single-instance 导致全进程卡死（LevelDB 锁竞争 + 主线程同步阻塞）。拆除改多进程：每个 .md 双击启动独立 solo.exe，独立 WebView2 数据目录（%TEMP%），互不牵连。Settings 用 tauri-plugin-store JSON + 文件锁，多进程安全。见 PROFILE.md §全进程卡死根因修复",

    // ── 窗口/生命周期 ──
    "onWindowEventSingleCallback": "Tauri v2 只支持一个回调→必须合并",
    "saveCurrentDocumentWait": "isSaving 锁改为等待≤1s 而非直接 false",
    "exitAppViaRustCommand": "app.exit(0) 通过 IPC 调用，不在主线程",

    // ── 文件关联 ──
    "installerHooksForFileAssoc": "Tauri fileAssociations 不写 ShellNew/DefaultIcon；Rust 运行时注册太晚。NSIS 安装阶段写注册表 + SHChangeNotify 刷新，装完即生效。见 PROFILE.md §文件关联",

    // ── Markdown roundtrip ──
    "serializerEscapeTwoModes": "clipboard=false（文件保存）严格转义 14 字符（移除 !?）保证 fidelity；clipboard=true（剪贴板）仅转义 \\ ` * + 行首特殊字符。见 serializer.ts:136-150",
    "nestedListIndentDepth": "每级嵌套 3 空格缩进（CommonMark 标准）。serializer.ts listDepth 计数",
    "taskListDetection": "parser bullet_list_open 前看 token 检测 task-list-item class → 开 taskList 代替 bulletList；checked 状态从 html_inline <input checked=\"\"> 提取，不从 class 属性（markdown-it-task-lists 仅 token 阶段不加 checked class）",
    "cjkBoldBoundaryZWNJ": "CJK 标点后 **/* 边界问题同根因：CommonMark scanDelims 以 Unicode 类别判定 right_flanking。Po/S 类使 canClose=false。serializer 为 bold 和 italic 插入 ZWNJ (Cf 类) 使 parser 正确识别关闭符，零副作用。纯 markdown roundtrip（不经编辑）退化为转义，这是规范限制。见 PROFILE.md §CJK 标点加粗边界",
  },

  "testInfrastructure": {
    "testFramework": "Vitest + happy-dom",
    "testCommand": "bun run test",
    "buildValidation": "vue-tsc --noEmit && vite build",
    "roundtripTestDir": "src/components/Editor/tiptap/markdown/__tests__/",
    "fixtureDir": "src/components/Editor/tiptap/markdown/__tests__/fixtures/",
    "fixtureCount": 17,
    "fuzzCount": 100,
    "coreFiles": {
      "testUtils": "src/components/Editor/tiptap/markdown/__tests__/test-utils.ts",
      "fixtureSpec": "src/components/Editor/tiptap/markdown/__tests__/fixtures.spec.ts",
      "fuzzSpec": "src/components/Editor/tiptap/markdown/__tests__/fuzz.spec.ts",
      "roundtripSpec": "src/components/Editor/tiptap/markdown/__tests__/roundtrip.spec.ts"
    },
    "testingStrategy": "两轮稳定性（parse→serialize→parse→serialize，round1===round2）而非精确匹配输入/输出。每改动 parser/serializer 后必须 bun run test"
  },

  "knownLimitations": [
    "表格内 <br> 不能换行 — GFM 标准限制",
    "Mark 顺序重排 — ProseMirror schema 行为",
    "PDF 导出 = 浏览器打印 — 改名诚实",
    "P3: 崩溃 .tmp 残留清理 — 方案复杂，搁置",
    "P4: 多进程内存占用偏高（每进程 ~80MB），合 3 窗口 ≈240MB。候选方案：(a) MemoryUsageTargetLevel=Low 零风险，收益一般；(b) TrySuspendAsync 失焦挂起渲染进程，收益大但有白屏/事件丢失风险。下次版本迭代时评估。",
    "CJK 标点后 **/* 关闭符：CommonMark scanDelims 视 Unicode 标点为 right_flanking=false。serializer 插入 ZWNJ 修复 editor→file→editor 回环；纯 markdown roundtrip（不经编辑）退化为 \\*\\*…\\*\\* 或 \\*…\\*，属规范限制，无法在 serializer 层根除。"
  ],

  "buildEnv": {
    "cargo": "Rust 1.96.0, CARGO_HOME=M:\\rust\\.cargo, RUSTUP_HOME=M:\\rust\\.rustup",
    "msvc": "Build Tools v14.44 at M:\\VS\\BuildTools",
    "bun": "1.3.14",
    "gitHubAccess": false,
    "buildScripts": {
      "fullBundle": "M:\\temp\\build_solo_tauri.bat",
      "rustOnly": "M:\\temp\\build_solo_full.bat",
      "frontendOnly": "M:\\temp\\build_solo_frontend.bat"
    }
  },

  "pendingItems": {
    "P3": "崩溃 .tmp 残留清理 — 搁置",
    "P4": "多进程内存优化 — 下次版本评估。候选：MemoryUsageTargetLevel=Low / TrySuspendAsync 窗口失焦挂起"
  },

    "latestSession": "2026-06-29 roundtrip 修复：serializer escapeInline 移除 !?（2 个失败归零）；CJK 边界 ZWNJ 覆盖 bold + italic（+3 失败归零 + reopen 场景）；79 roundtrip tests 全绿，357 total tests 全绿，vue-tsc + vite build 无错误"
}
