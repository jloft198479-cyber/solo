---
title: solo 互链方案 · 执行记录
type: archive
audience: maintainer
status: active
tags: [执行记录, 互链, wikilink, 拖拽, 回滚锚点]
summary: 互链方案落地全过程的执行/自检/回滚记录，执行人 qianwen
updates: [docs/solo互链方案-2026-09-11.md]
---

# solo 互链方案 · 执行记录

**执行人**：qianwen
**日期**：2026-09-11（Asia/Shanghai）
**方案文档**：[docs/solo互链方案-2026-09-11.md](./solo互链方案-2026-09-11.md)（v5）

---

## 0. 回滚基线（执行前状态）

| 项 | 值 |
|---|---|
| 分支 | `master` |
| 基线 HEAD（执行前） | `a90907e` docs: 把自动发版踩坑经验固化进 RELEASE_PROCESS.md §11 |
| 版本 | package.json `1.2.50` |
| 执行前工作树 | 干净，仅 `docs/solo互链方案-2026-09-11.md` 未跟踪；无并行会话 WIP |
| 回滚方式 | 逐阶段独立提交；任一步出问题 `git revert <该步commit>`，或整体 `git reset --hard a90907e` 回到基线 |

**约定**：一期只做 Phase 1–4；每个 Phase 一个（或数个）独立提交，改测试/文档随码提交；提交只 `git add` 显式路径，不 `add -A`。Rust 侧本期不动。

---

## 1. Phase 1 · 修 `[[` 弹候选接线 ✅

- **改动**：`editor-extensions.ts` `WikilinkSuggest.configure(...)` 顶层补 `getDocumentPath: () => options.getDocumentPath?.() ?? null`，把当前文档路径接进扩展级 option（`allow` 门控据此判断是否弹 `[[` 菜单）。
- **根因**：v1.2.43（`6a28edd`）引入 `[[` 补全时，`suggestion` 闭包用了 `options.getDocumentPath` 但扩展级 `getDocumentPath` 从没接，恒为默认 `()=>null` → 菜单对所有保存状态永不弹。
- **测试**：`editor-extensions.spec.ts` 加回归锁，断言 `createEditorExtensions` 产出的 `wikilinkSuggest` 扩展级 `getDocumentPath()` 返回注入路径。
- **自检**：`vitest`（wikilink/wikilink-suggest/editor-extensions 共 25 项）全绿；`vue-tsc --noEmit` 退出 0。
- **回滚**：本阶段独立提交，`git revert` 即可；未碰其它入口。

## 2. Phase 2 · 修 `![..](..)` 被链接直输误吃 ✅

- **改动**：`markdown-input.ts` `convertPendingLink` 在拿到 `match` 后加守卫——`[` 起点前紧邻字符为 `!` 时判为图片语法，直接 `return null` 不转换（与 `suggestion-guard.ts` 的 `![[` 守卫同款）。
- **根因**：`linkInputRegex` 无 `!` 前缀判别，敲 `![说明](路径)` 会被匹配 `[说明](路径)`（index=1），转换后残成 `!` + 半条链接，属"边敲边坏"。
- **测试**：`markdown-input.spec.ts` 加回归——`![图](url)` 保持字面不变；同段普通 `看 [docs](url)` 仍正常转链接（守卫生效不误伤）。
- **自检**：`vitest`（markdown-input + ime 共 33 项）全绿；`vue-tsc --noEmit` 退出 0。
- **回滚**：本阶段独立提交，`git revert` 即可。

## 3. Phase 3 · 拖入即互链（看落点，方案 B）✅（落点插入已补齐，坐标待真机验证）

- **决策纯函数**：新增 `wikilink-drop.ts` `decideDocumentDrop(paths, currentDocPath, isInsideEditor)` → `insert | fallback`（收口后两态，见 §6）。一期口径：仅「落在正文内 + 当前已保存 + 被拖为同目录 `.md/.markdown` + 非自身」才 insert；`.txt` / 跨目录 / 未保存 / 正文外 / 拖自身 / 落点在代码块内一律 fallback（安全默认=现状）。`isSameDirectory` 归一化后精确比较（宁漏判回落 fallback，不误链）。
- **接线四处**：
  - `MarkdownEditor.vue`：`handleDocumentDrop(paths, position)`（命中测试 → 决策 → 在**落点**插 `wikilink` 节点）+ `posFromDropPoint`（物理像素÷devicePixelRatio 后 `elementFromPoint` 命中编辑器 DOM，再用 `view.posAtCoords` 反查文档位置，取不到则退回当前光标）；`defineExpose` 暴露。
  - `useAppWindowSession.ts`：新增可选 `requestWikilinkDrop`，drop 回调先问编辑器、被接住则不 open。
  - `useAppEditorState.ts`：`AppEditorExpose` 加 `handleDocumentDrop?` 类型。
  - `App.vue`：注入 `requestWikilinkDrop → editorRef.handleDocumentDrop`。
- **测试（四道锁）**：`wikilink-drop.spec.ts` 覆盖 insert/fallback 各分支（正文内外、同/跨目录、.txt、未保存、混拖图片、多文件、拖自身、分隔符归一化、`isDocumentPath`）。
- **自检**：全量 `bun run test` 通过、`vue-tsc --noEmit` 退出 0、`vite build` 成功（chunk 大小告警为既有、非错）。
- **⚠️ 未真机验证项**：拖拽落点 `position` 的类型（物理 vs 逻辑像素）、标题栏偏移——`posFromDropPoint` 按「÷dpr + elementFromPoint + posAtCoords」实现并已在代码注释标注。**失败模式非破坏性**：误判为「不在正文」→ 回落「打开」（现状）；误判为「在正文」→ 插入一条可撤销的链接，不写文件、不越界；落点取不到 → 退回落光标。需真机确认命中是否跟手，稳则收工、不稳则按方案改 Alt 兜底。
- **未做（守一期范围）**：悬停提示、子目录递归、跨盘/绝对路径、Rust 改动（同目录不需动 `list_markdown_files`）。
- **回滚**：本阶段独立提交，`git revert` 即可整体撤掉拖入功能、不影响 Phase 1/2。

## 4. Phase 4 · 文档联动 + 发版三关 + 登记 ✅

按 AGENTS §七「改 X 必查 Y」联动矩阵回写：
- `docs/CHANGELOG.md`：新增 `[Unreleased]` 段（Added 拖入成链；Fixed `[[` 接线、`![` 误吃），如实标注拖拽未真机验证。
- `docs/KNOWN-ISSUES.md`：§一 加 #21（`[[` 接线漏递）、#22（`![` 误吃）；§四 加两条文档坑（Suggestion 扩展级 option 接线陷阱、`v1.2/B` 误报澄清）。
- `docs/INDEX.md`：登记 `solo互链方案-2026-09-11.md`（proposal/active）与 `执行记录-互链方案-2026-09-11.md`（archive）。
- `wiki/编辑器功能指南.md`：更新 WikiLink 行（`[[` 补全已可用 + 拖入同目录成链）。
- `ARCHITECTURE.md` §11 敏感区速查：加 #14（扩展级 option 接线 + 拖拽落点路由）。
- **死链自检**：新增文件均存在；跨文档链接指向有效（`#21/#22`、新模块路径、两份新文档）。
- **Rust**：本期未动 `src-tauri`，同目录方案不需 `list_markdown_files` 递归 → 无 `cargo check` 需求。

**发版三关（对全量代码，Phase 3 提交后跑）**：`bun run test` **1277 全过** → `vue-tsc --noEmit` **退出 0** → `vite build` **成功**。Phase 4 仅改 `.md`，不影响三者。

## 5. 总结与遗留

**已落地（4 个独立可回滚提交，基线 a90907e）**：
| commit | 内容 |
|---|---|
| `d9150b1` | docs：方案 v5 + 执行记录基线（回滚锚点） |
| `e3658a0` | Phase1：修 `[[` 弹候选接线 + 回归锁 |
| `6854003` | Phase2：修 `![..](..)` 被误吃 + 回归锁 |
| `157602d` | Phase3：拖入同目录即在光标处成互链（决策纯函数 + 四道锁） |
| （Phase4） | docs：联动矩阵回写 + 登记 |

**遗留 / 待办（不阻塞）**：
1. ⚠️ **拖拽落点须真机验证**（唯一未证项）：验证「拖同目录 `.md` 进正文＝插链接、拖到正文外＝打开、拖图片仍插图」是否跟手；坐标换算（物理÷dpr、标题栏偏移）若不稳，按方案回落到 Alt 修饰键兜底。**在真机确认前，此项标注「未验证」，不得视为已提升体验。**
2. C 档符号入口（表格 / 彩色块 / 脚注 / 待办勾选）的**敲字即时转换**：静态看均无 `addInputRules`、大概率"敲了没反应"而非"变坏"——按一期第一性原则（只修会坏、不为对齐而加）**默认不补**；若真机眼验发现某项"变坏"再单开一条修。`---` frontmatter 已判否（被分割线抢占，属结构事实）。
3. 二期候选（均需另做产品级决定，未启动）：子目录递归（改 Rust + 上限语义）、悬停提示（先扩事件层）、跨盘绝对路径 + 放开安全闸门、反向链接/图谱面板。（**落点精确插入已在一期收口时实现**，不再列为二期。）

---

## 6. 审核收口补记（Buddy 审核 → 2026-09-11 二次收口）

> 对 Phase 1–4 落地代码做产品级审核后的收口，独立于 qianwen 原提交；审核明细见 `.workbuddy/memory/2026-09-11.md`。

| 项 | 问题（审核发现） | 收口做法 |
|---|---|---|
| 假绿测试 | `wikilink-drop.spec.ts`「尾分隔符」一条的参数与上一条完全相同 → `dirOf` 去尾分支零覆盖 | 换真实参数（重复/混合分隔符），并抽出 `normalizePath` 统一分隔符归一 |
| 自链接 | 拖当前文档自身 → 生成指向自己的 `[[a]]`；而 `[[` 补全明确排除自身，两条入口不一致（已 node 实证） | `decideDocumentDrop` 归一化路径后排除自身 + 回归锁（仅大小写形式不同仍按不同文件处理，保守） |
| 落点降级 | 方案承诺「看落点」，实现只判「在/不在正文内」，位置永远用当前光标 → 拖到第 50 行会插在第 3 行 | `posFromDropPoint` 用 `view.posAtCoords` 反查落点 → `insertContentAt`；取不到回落光标 |
| 代码块无守卫 | `[[` 补全 `allow` 有 `codeBlock` 守卫，拖入路径无 → 代码块内拖 `.md` 只能靠 schema 兜底 | 落点（或光标）解析在 `codeBlock` 内则回落 fallback |
| 结构冗余 | `{kind:'open', path}` 的 `path` 生产代码从不读；窗口层与图片拖入各自内联 `/\.(md\|markdown\|txt)$/i`，与 `DOC_EXT` 同一事实三处 | 导出 `isDocumentPath` 单一定义三处共用；`DropDecision` 收窄为 `insert \| fallback`；`posInTable` 泛化为 `posInNode` |
| 文档脱节 | 方案文档 status 仍是 `proposal`、正文写「未动源码」；执行记录称方案 v5、方案自称 v4 | 方案 status → `archive` + 状态行改为「v5 定稿，已实施」；正文「仍不动源码」更正；INDEX 同步 |

- **验证（收口后重跑）**：`vitest` 43 文件 **1281 项全过**（较原 1277 增 4 项）→ `vue-tsc --noEmit` 退出 0 → `vite build`（`.sandbox-build`，验完清理）成功 15.33s。Rust 未动，无 `cargo check` 需求。
- **仍未做**：拖拽落点坐标真机验证（§5 遗留 1）；C 档符号入口眼验（原方案 Phase 0，实际未前置）。

**回滚**：任一阶段 `git revert <该步commit>`；整体回基线 `git reset --hard a90907e`。

> 执行人：**qianwen** ｜ 日期：2026-09-11 ｜ 源码基线 a90907e → Phase 1–4 全部落盘（Phase 4 提交见本记录最后一次 commit）。
